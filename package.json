{
  "name": "arbor",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "test": "npm run test --workspace server",
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/*/spawn-helper server/node_modules/node-pty/prebuilds/*/spawn-helper 2>/dev/null || true"
  }
}
